machine:
  node:
    version: 8.11.2
general:
  branches:
    only:
      - master
dependencies:
  pre:
      - npm install -g surge
test:
  override:
    - echo "Only need to deploy!"
deployment:
  production:
    branch: master
    commands:
      - yarn build
      - yarn export
      - surge out uiuc.hack4impact.org
